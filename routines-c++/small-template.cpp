#include <vector>
#include <string>
#include <set>
#include <map>
#include <deque>
#include <algorithm>
#include <cstdio>
#include <cmath>
#include <cctype>

using namespace std;

typedef long long ll;
typedef vector<int> vi;
typedef vector<string> vs;
typedef vector<double> vd;
typedef vector<vi> vvi;
typedef pair<int,int> pii;
typedef pair<double,double> pdd;
typedef vector<pii> vpii;
typedef vector<pdd> vpdd;

#define fr(i,j,k) for(int i=j;i<k;++i)
#define fir(n) fr(i,0,n)
#define fjr(n) fr(j,0,n)
#define fkr(n) fr(k,0,n)
#define flr(n) fr(l,0,n)
#define frd(i,j,k) for(int i=j;i>=k;--i)

#define si size()
#define pb(x) push_back(x)
#define mp(x,y) make_pair(x,y)

#define all(x) x.begin(),x.end()
#define del(x,y) memset(x,y,sizeof(x))

#define INF 1000000000
#define EPS 1e-9

//makevector<vector>: vvi x=vvi(r,vi(c,v)); // r=rows, c=columns, v=value

int dx[]={-1,0,1,0},dy[]={0,-1,0,1}; // 4-ways
int dx[]={1,1,1,0,-1,-1,-1,0},dy[]={1,0,-1,-1,-1,0,1,1}; // 8-ways
int dx[]={1,1,2,2,-1,-1,-2,-2},dy[]={2,-2,1,-1,2,-2,1,-1}; // knight

class  {
public:


};